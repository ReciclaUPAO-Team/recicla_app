version: 1.0
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build -- --configuration production
  artifacts:
    baseDirectory: dist/recicla_upao
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

